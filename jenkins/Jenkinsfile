pipeline {

  agent {
    docker {
      image 'gradle:alpine'
      args '-p 8888:8888 -v /home/gradle:/home/gradle -v /var/jenkins_home/workspace/:/var/jenkins_home/workspace'
    }
  }

  stages {

    stage('Build') {
      steps {
        sh 'gradle clean assemble'
      }
    }

    stage('Test') {
      steps {
        sh 'gradle test'
      }
      post {
        always {
          junit 'build/test-results/test/*.xml'
        }
      }
    }

    stage('Test-BDD') {
      steps {
        sh 'gradle cucumber'
      }
    }


  }

}
